# Agentic-RL 项目完整方案蓝图

> 基于 `方案文档写作蓝图.md` 前半部分 + 核心决策补全后半部分
> 生成时间：2026-04-04

## 1. Context（3-5行）

985本硕学生，目标大厂后训练算法岗实习。项目核心：基于 GRPO + 执行反馈的代码生成强化学习，从单轮到多轮纠错。本地 5070Ti 12GB 先跑通流程，再上云端 80GB GPU 跑正式实验。技术栈：veRL 框架 + Qwen3 模型（内置思维链，跳过SFT）+ 门控奖励 + AgentLoop 多轮交互。

## 2. 技术选型决策表

| 组件 | 本地验证期（5070Ti 12GB） | 云端正式期（80GB GPU） | 选择理由 |
|------|--------------------------|----------------------|---------|
| 基座模型 | Qwen3-1.7B | Qwen3-4B | Qwen3 内置思维链，无需SFT；4B 在 80GB 上用 bf16+LoRA 舒适 |
| 训练框架 | veRL | veRL | 字节开源，原生支持 GRPO + AgentLoop，全流程统一 |
| LoRA rank | r=8, alpha=16 | r=16, alpha=32 | 本地省显存，云端加容量 |
| GRPO num_generations | 4 | 8~16 | 本地显存限制 |
| max_completion_length | 512 | 4096 | 本地省显存 |
| Rollout 后端 | HF generate | vLLM | 本地省显存；云端提速推理 |
| 沙盒 | async subprocess（asyncio） | 同左 | veRL AgentLoop 是 async，必须配套 |
| Reward | 门控奖励（Gating Reward） | 同左 | 代码提取失败→格式分归零，无部分分 |
| 评估 | HumanEval+ | HumanEval+ + MBPP+ + LiveCodeBench | 本地快速验证；云端完整评估 |
| 训练数据 | MBPP train+val (~464条) | MBPP train+val + APPS-easy-cleaned (~3000条) | MBPP 自带 assert；**不用 test split（避免与 MBPP+ 评估数据重叠）**；APPS 经 LLM 清洗 + 沙盒验证 |
| 日志 | TensorBoard | TensorBoard + WandB | 本地轻量，云端可远程查看 |
| 云平台 | — | AutoDL（80GB GPU） | 性价比高，ModelScope 内网加速下模型 |
| 容器环境 | WSL2 + Docker Desktop | AutoDL 实例直接安装 | 本地隔离依赖；云端已是容器 |

## 3. 项目目录结构

```
agentic-rl/
├── configs/
│   ├── local_grpo_single.yaml
│   ├── local_grpo_multi.yaml
│   ├── cloud_grpo_single.yaml
│   └── cloud_grpo_multi.yaml
├── data/
│   ├── raw/
│   ├── grpo_train.parquet
│   └── README.md
├── src/
│   ├── __init__.py
│   ├── sandbox.py              # async 代码沙盒
│   ├── reward.py               # 门控奖励函数
│   ├── data_prepare.py         # 数据准备
│   ├── train_grpo_single.py    # 阶段一：单轮 GRPO
│   ├── train_grpo_multi.py     # 阶段二：多轮 AgentLoop GRPO
│   ├── evaluate.py             # 统一评估
│   └── utils.py                # 工具函数
├── scripts/
│   ├── setup_local.sh
│   ├── setup_cloud.sh
│   ├── run_local.sh
│   ├── run_cloud.sh
│   └── synth_testcases.py     # APPS 数据清洗（LLM API + 沙盒验证）
├── analysis/
│   ├── plot_training.py        # 训练曲线可视化
│   └── plot_comparison.py      # 消融对比图
├── eval_results/
├── outputs/
│   ├── grpo_single/
│   └── grpo_multi/
├── logs/
├── tests/
│   ├── test_sandbox.py
│   └── test_reward.py
├── Dockerfile
├── requirements.txt
└── README.md
```

## 4. 环境搭建

### 4.1 Docker 镜像构建
- 基于 `nvidia/cuda:12.4.0-devel-ubuntu22.04`
- Python 3.10、PyTorch cu124、veRL、evalplus、datasets、pyarrow
- 核心函数签名：无（Dockerfile 配置）
- 需要配置：Docker Desktop WSL2 backend + NVIDIA Container Toolkit

### 4.2 本地环境（WSL2 + 5070Ti 12GB）
- 安装 Docker Desktop + WSL2 backend + NVIDIA Container Toolkit
- 构建镜像 → 验证 GPU 直通
- 验证脚本：检查 CUDA、GPU、veRL import

### 4.3 云端环境（AutoDL 80GB GPU）
- PyTorch + CUDA 12.4 基础镜像
- 直接 pip install veRL + 依赖（不套 Docker）
- ModelScope 内网加速下载 Qwen3-4B
- tmux 防断连

## 5. 阶段零：基础设施搭建（本地，2-3天）

### 5.1 代码执行模块 `src/sandbox.py`
- 核心函数签名：
  - `async execute_code(code: str, test_cases: str, timeout: float = 5.0) -> tuple[bool, str]`
  - `async execute_batch(codes: list, test_cases_list: list, timeout: float = 5.0) -> list[tuple[bool, str]]`
- 枚举：`ExecStatus`（SUCCESS / SYNTAX_ERROR / RUNTIME_ERROR / TIMEOUT）
- 数据类：`ExecResult(status, stdout, stderr)`
- 使用 `asyncio.create_subprocess_exec`（因 veRL AgentLoop 是 async）

### 5.2 数据准备 `src/data_prepare.py`
- 核心函数签名：
  - `load_mbpp_data() -> Dataset`
  - `load_apps_cleaned(max_samples: int = 3000) -> list[dict]`
  - `create_verl_parquet(data: list[dict], output_path: str) -> None`
- 数据格式：veRL parquet 格式（prompt 列为 chat messages JSON 字符串 + extra_info 列含 test_cases）
- Qwen3 system prompt：利用原生思考模式，要求输出 `思考过程 + ```python 代码块`
- 本地：MBPP train+val (~464条)；云端：MBPP train+val + APPS-easy-cleaned (~3000条)
- **为什么不用 MBPP test split**：test split 的题目与 MBPP+ 评估 benchmark 重叠，用 test split 训练会在 MBPP+ 上造成数据泄露。因此只用 train+val（464条），通过 APPS 数据扩充到 ~3000 条。
- **APPS 数据清洗（建议做）**：用 `scripts/synth_testcases.py` 通过 DeepSeek-V3 API 将 APPS input/output 转为 assert，再用沙盒 + reference solution 验证。升级为"建议做"的理由：
  1. 不用 test split 后只有 464 条训练数据，batch_size=128 × 200 steps = 每条数据被采样 ~55 次，有过拟合风险
  2. 加 APPS 后 ~3000 条，每条被采样 ~8.5 次，更健康
  3. 清洗过程本身展示数据工程能力（LLM 生成 + 沙盒验证 + reference 校验），面试加分
- **MBPP 数据质量检查（可选）**：MBPP 有已知噪声（test_list 引用未定义函数名等）。建议在加载后用 reference solution + test_cases 在沙盒中验证，过滤掉 test case 本身有问题的样本（约 5-10%）。此步不阻塞主线。

### 5.3 奖励函数 `src/reward.py`（门控设计 + 二元稀疏）
- 核心函数签名：
  - `extract_code(text: str) -> Optional[str]`
  - `compute_score(data_source: str, solution_str: str, ground_truth: str, extra_info: dict) -> float`
- 二元稀疏奖励逻辑：
  - 代码提取失败 → **-1.0**（重罚，逼迫模型输出代码块）
  - 语法错误 → **0.0**
  - 超时 → **0.0**
  - 运行时错误 / assert 失败 → **0.0**
  - 测试通过 → **+1.0**
- 为什么用二元稀疏而非阶梯负分：阶梯负分会诱导模型学"不出力"的策略（如输出 `def f(): pass` 拿 -0.2 比努力写代码但语法错误拿 -0.3 更高）。二元奖励消除了所有中间作弊空间，GRPO 的组内归一化 (r-mean)/std 会让 0.0 和 1.0 产生足够的梯度信号。

### 5.4 验证测试
- `tests/test_sandbox.py`：6 个测试用例（正常、语法错、运行时错、超时、带测试用例、批量）
- `tests/test_reward.py`：7 个测试用例（代码提取、完美输出、无代码、代码错误、乱输出、语法错误、超时代码）

---

## 6. 阶段一：单轮 GRPO + 执行奖励（本地验证 + 云端正式，1-1.5周）

### 目标
跳过 SFT，直接对 Qwen3 基座模型做 GRPO 训练，用门控执行奖励引导代码生成。

### 6.1 训练脚本 `src/train_grpo_single.py`
- 核心函数签名：
  - `main(config_path: str) -> None`
- 关键逻辑：
  - 加载 Qwen3-1.7B/4B + LoRA 配置
  - 加载 parquet 训练数据
  - 配置 veRL GRPO 训练器：传入 `compute_reward` 作为自定义 reward function。注意：veRL 的架构中，奖励函数通常并不是在 Python 初始化 Trainer 时像普通参数那样传进去的。标准的做法是，在 configs YAML 文件中，配置一个 reward_fn 的路径，指向写好的那个 src/reward.py 里的特定方法。Ray Worker 在分布式执行时，会根据这个路径动态导入（import）代码模块。这能确保在多个并发的 Docker/子进程沙箱中都能正确加载打分逻辑。
  - 本地 rollout 后端：HF generate（省显存）
  - 云端 rollout 后端：vLLM（加速推理）
- 需要的 veRL 配置项（通过 yaml 或代码设置）：
  - `actor_rollout_ref.model.path`
  - `actor_rollout_ref.rollout.name`（HF generate 或 vLLM）
  - `reward.model.path` 或自定义 reward function 注册
  - `algorithm.name: "grpo"`
  - `trainer.total_training_steps`
  - LoRA rank/alpha
  - `rollout.tensor_model_parallel_size`

### 6.2 配置文件
- `configs/local_grpo_single.yaml`
  - model: Qwen3-1.7B
  - LoRA r=8, alpha=16
  - num_generations=4
  - max_completion_length=512
  - max_steps=100
  - rollout backend: HF generate
  - learning_rate=5e-6
  - tensorboard logging

- `configs/cloud_grpo_single.yaml`
  - model: Qwen3-4B
  - LoRA r=16, alpha=32
  - num_generations=16
  - train_batch_size=128（匹配 ~3000 条数据，避免 epoch 过多导致过拟合）
  - max_completion_length=4096
  - max_steps=200
  - rollout backend: vLLM（async mode）
  - learning_rate=1e-6
  - seed=42
  - save_freq=50（保存 4 个 checkpoint，事后逐一评估）
  - KL loss: use_kl_loss=True, kl_loss_coef=0.001
  - wandb logging

### 6.3 运行脚本
- `scripts/run_local.sh`：一键运行本地训练
- `scripts/run_cloud.sh`：tmux 内运行云端训练
- 关键命令格式：
  - `python src/train_grpo_single.py config=configs/local_grpo_single.yaml`

### 6.4 监控指标

| 指标 | 健康范围 | 异常信号 |
|------|---------|---------|
| `reward/mean` | 持续上升 | 停滞/下降 → reward hacking 或 collapse |
| `reward/std` | > 0.1 | 接近 0 → 输出趋同，缺乏多样性 |
| `kl` (KL 散度) | 0.1 ~ 5.0 | > 10 → 偏离参考策略太远 |
| `loss` | 持续下降 | 突然飙升 → 训练不稳定 |
| `completion_length/mean` | 逐步收敛 | 突然归零 → 模型拒绝生成 |

- 本地：`tensorboard --logdir outputs/grpo_single --bind_all`
- 云端：WandB dashboard 自动记录

### 6.5 阶段一评估
- 训练结束后立即评估
- 命令：`python src/evaluate.py --config ... --model_path outputs/grpo_single/final --tag grpo_single`
- 验收标准：HumanEval pass@1 相比 baseline 有提升（哪怕 1-2% 也是正向信号）
- **本地不要求大幅提升**（1.7B 能力有限），重点是 pipeline 跑通 + reward 曲线上升

---

## 7. 阶段二：多轮 Agentic GRPO（1.5-2周，核心创新）

### 目标
模型第一次执行失败后，接收错误反馈，进行自我纠正。使用 veRL 原生 AgentLoop 实现真正的多轮交互。

### 7.1 AgentLoop 实现 `src/agent_loop.py`（新增文件）
- 核心函数签名：
  - `async def code_agent_loop(model, tokenizer, prompt, test_cases, max_turns=3, turn_penalty=0.15, timeout=5.0) -> AgentLoopResult`
- 数据类：
  - `AgentLoopResult(final_code: str, reward: float, turns: int, history: list[dict])`
- 工作流程：
  1. 模型生成代码 → async 沙盒执行
  2. 成功 → reward = +1.0，结束
  3. 失败 → 构造错误反馈消息，拼入对话历史
  4. 模型基于错误反馈再次生成 → 执行 → 判断
  5. 重复直到成功或达到 max_turns
- 错误反馈模板：
  - 包含错误类型 + stderr（**尾部截断**至 500 字符，Python traceback 最有用信息在底部）
  - **AssertionError 脱敏**：隐藏具体参数值，防止模型拟合测试用例
  - 提示模型修正代码
- 与 veRL 集成方式：
  - veRL AgentLoop 支持自定义 async function
  - 注册为 veRL 的 rollout hook

### 7.2 多轮奖励设计
- 核心原则：成功越早越好
- 奖励公式：
  - 第 1 轮成功：+1.0
  - 第 2 轮成功：+1.0 - turn_penalty
  - 第 3 轮成功：+1.0 - 2 × turn_penalty
  - 全部失败：按单轮门控逻辑给负分（取最低分的那轮）
- turn_penalty 默认 0.15（可调参）
- 门控逻辑在多轮中同样生效：任何轮次代码提取失败 → 该轮 -1.0
- **纯稀疏奖励**：只看最终执行结果 + turn_penalty，不设中间进步补偿
  （避免 RL agent 学会"故意先写错再改"来骗取中间奖励）

### 7.3 训练脚本 `src/train_grpo_multi.py`
- 核心函数签名：
  - `main(config_path: str) -> None`
- 关键逻辑：
  - 加载阶段一输出的模型（作为起点）
  - 配置 veRL AgentLoop rollout
  - 传入自定义 `code_agent_loop` 函数
  - 其余 GRPO 配置同阶段一，但：
    - `max_completion_length` （3072）
    注意：
    监控前 50 步的显存水位： 在 AutoDL 上启动任务后，开一个终端盯着 watch -n 1 nvidia-smi。

如果峰值显存卡在 60GB 左右波动，说明完美压榨。

如果显存只吃到了 40GB，说明绝大部分样本根本没用到 3072 这么长。这时你甚至可以通过增大 num_generations（比如从 16 提到 24）来进一步提速。
    - `learning_rate` 略降（3e-6，多轮更敏感）
    - 训练数据使用同阶段一

### 7.4 配置文件
- `configs/local_grpo_multi.yaml`
  - model_path: `outputs/grpo_single/final`
  - max_turns=2
  - max_completion_length=512
  - turn_penalty=0.15
  - 其他同 local_grpo_single

- `configs/cloud_grpo_multi.yaml`
  - model_path: `outputs/grpo_single/final`
  - max_turns=3
  - max_completion_length=3072
  - turn_penalty=0.15
  - 其他同 cloud_grpo_single

### 7.5 阶段二评估
- 同阶段一评估流程
- 额外关注：多轮成功率（第几轮通过的比例）、
纠错转化率 ：
定义： 在第一轮执行失败的所有样本中，有多少比例在第二轮或第三轮转为成功。
意义： 这个指标直接量化了 Agentic RL 到底有没有让模型学会“理解报错”和“自我修正”，而不仅仅是靠运气在多抽几次样。
- 验收标准：多轮模型 pass@1 > 单轮模型 pass@1

---

## 8. 统一评估脚本

### `src/evaluate.py`
- 核心函数签名：
  - `generate_for_benchmark(model, tokenizer, benchmark: str, output_file: str, max_new_tokens: int, num_samples: int = 1, temperature: float = 0.0) -> str`
  - `run_evalplus(samples_file: str, benchmark: str, k_values: list = None) -> dict`
  - `run_livecodebench(model, tokenizer, max_new_tokens: int) -> dict`
  - `main() -> None`
- 支持的 benchmark：
  1. **HumanEval+**（164题 + EvalPlus 增强测试）：`evalplus.evaluate --dataset humaneval`
  2. **MBPP+**（500题 + EvalPlus 增强测试）：`evalplus.evaluate --dataset mbpp`
  3. **LiveCodeBench**（定期更新的竞赛题）：从 LiveCodeBench 加载最新题目
- 关键特性：
  - 断点续传（已完成的 task_id 跳过）
  - 默认贪心解码（temperature=0.0）用于 pass@1
  - 支持多采样（`--num_samples 10 --temperature 0.6`）用于 pass@10
  - evalplus 通过 `--min-seq` 参数自动计算 pass@k
  - 结果保存为 `eval_results/{benchmark}_{tag}.jsonl`
- 命令行参数：
  - `--model_path`：模型路径
  - `--tag`：实验标签（baseline / grpo_single / grpo_multi 等）
  - `--benchmarks`：可选，指定 benchmark 子集
  - `--num_samples`：每题生成样本数（默认 1，pass@10 时设为 10）
  - `--temperature`：采样温度（默认 0.0，pass@10 时建议 0.6）
注意：在 generate_for_benchmark 函数中，必须复用 extract_code(text) 函数。只有经过正则提取、纯净无杂质的 Python 代码，才能被写入最后的 .jsonl 评测文件中
---

## 9. 消融实验（必做 3 组 + 结果表格模板）

### 9.1 消融 A：奖励设计对比
- **实验组 1**：门控奖励（Gating Reward，本项目默认）
- **实验组 2**：加法奖励（Additive Reward = 0.2 × format + 0.8 × execution）
- 对比维度：HumanEval+ / MBPP+ pass@1
- 需要修改：`src/reward.py` 中新增 `compute_reward_additive()` 函数
- 新增配置：`configs/cloud_grpo_single_additive.yaml`

### 9.2 消融 B：num_generations 影响
- **实验组 1**：num_generations=4
- **实验组 2**：num_generations=8
- **实验组 3**：num_generations=16
- 目的：验证 GRPO 组内对比数量对性能的影响
- 需要修改：仅修改配置文件的 `num_generations` 字段

### 9.3 消融 C：单轮 vs 多轮
- **实验组 1**：仅单轮 GRPO（阶段一输出）
- **实验组 2**：单轮 + 多轮 GRPO（阶段二输出）
- 目的：验证多轮纠错带来的增益
- 无需额外训练，直接对比阶段一和阶段二的评估结果

### 9.4 结果表格模板

| 模型配置 | HumanEval pass@1 | HumanEval+ pass@1 | MBPP+ pass@1 | LiveCodeBench | HumanEval pass@10 |
|---------|------------------|--------------------|--------------|---------------|-------------------|
| Baseline (Qwen3-4B，未训练) | X% | X% | X% | X% | X% |
| + 单轮 GRPO（门控奖励） | X% | X% | X% | X% | X% |
| + 单轮 GRPO（加法奖励） | X% | X% | X% | X% | X% |
| + 单轮 GRPO + 多轮 GRPO | X% | X% | X% | X% | X% |
| num_gen=4 vs 8 vs 16 对比 | — | — | — | — | — |

> **pass@10 说明**：pass@10 使用 `--num_samples 10 --temperature 0.6` 多采样评估，衡量模型"10 次尝试中至少有 1 次通过"的概率。pass@10 远高于 pass@1 说明模型有正确的解题方向但生成不够稳定。

---

## 10. 训练动态分析与可视化

### 10.1 关键追踪指标
- **reward/mean**：训练过程中平均奖励变化趋势
- **reward/std**：奖励标准差（反映生成多样性）
- **kl**：策略与参考模型的 KL 散度（反映偏离程度）
- **thinking_length/mean**：Qwen3 思维链平均 token 数变化
- **completion_length/mean**：生成长度变化
- **pass_rate/step**：每 N 步的测试通过率（需要定期抽样评估）

### 10.2 可视化脚本
- `analysis/plot_training.py`
  - 核心函数签名：
    - `plot_reward_curve(log_dir: str, output_path: str) -> None`
    - `plot_kl_divergence(log_dir: str, output_path: str) -> None`
    - `plot_thinking_length(log_dir: str, output_path: str) -> None`
    - `plot_all(log_dir: str, output_dir: str) -> None`
  - 输入：TensorBoard/WandB 日志目录
  - 输出：PNG 图表
  - 使用 matplotlib，从 TensorBoard event 文件或 WandB API 提取数据

- `analysis/plot_comparison.py`
  - 核心函数签名：
    - `plot_ablation_bar(results: dict, output_path: str) -> None`
    - `plot_multi_model_radar(results: dict, output_path: str) -> None`
  - 输入：各模型的评估结果 JSON
  - 输出：柱状图（消融对比）、雷达图（多维度对比）

- `analysis/plot_thinking_analysis.py`
  - 核心函数签名：
    - `load_metrics(log_path: str) -> list[dict]`
    - `plot_thinking_vs_reward(records: list, output_path: str) -> None`
    - `plot_thinking_over_time(records: list, output_path: str, window: int) -> None`
    - `print_statistics(records: list) -> None`
  - 输入：训练时 reward.py 自动记录的 `logs/thinking_metrics.jsonl`
  - 输出：散点图（thinking_tokens vs reward）、趋势图（thinking length 随训练步数变化）
  - 关键分析：thinking 越长代码越好？还是先增后减（模型先学会仔细分析，再内化能力）？

### 10.3 Thinking Length 采集机制

在 `src/reward.py` 的 `compute_score()` 中，每次打分时追加记录：
- `thinking_tokens`：去掉代码块后的文本长度，粗略估算 token 数
- `reward`：最终奖励分数
- `has_code`：是否成功提取代码
- 写入 `logs/thinking_metrics.jsonl`（每行一条 JSON）

训练完成后运行 `python analysis/plot_thinking_analysis.py --log logs/thinking_metrics.jsonl` 即可生成分析图表。

### 10.4 面试话术要点
- "训练过程中 reward 曲线从 -0.3 上升到 +0.6，说明模型逐步学会了通过测试用例"
- "KL 散度始终控制在 3.0 以内，通过学习率和梯度裁剪防止策略偏离"
- "多轮训练中思维链长度先增后减，说明模型先学会分析错误、再学会高效纠错"

---

## 11. 完整执行顺序清单

### 本地阶段（5070Ti 12GB，验证 pipeline）

```
1.  构建 Docker 镜像 → 验证 GPU 直通

AgentLoop 早期验证（步骤 1 完成后立即做！）
──────────────────────────
1.5 测试 veRL AgentLoop 最小示例（只依赖 veRL 安装，不依赖其他代码）
    python3 -c "from verl.workers.agent.agent_loop import AgentLoopBase; print('OK')"
    → 成功 → 继续后续步骤
    → 失败且无法修复 → 立即切 Plan B（Section 15.1）
    → 不在 AgentLoop 调试上花超过 1 天

2.  运行 src/data_prepare.py → 生成 grpo_train.parquet
3.  运行 tests/test_sandbox.py → 验证 async 沙盒
4.  运行 tests/test_reward.py → 验证门控奖励

阶段零点五：端到端验证（1-2小时，在阶段一之前必须完成！）
──────────────────────────
4.5 运行 scripts/verify_e2e.py → 手动跑 prompt→generate→extract_code→sandbox→reward 全流程
    → 确认 extract_code 正确提取代码块
    → 确认沙盒执行返回正确状态
    → 确认 reward 计算结果正确（正确代码=1.0, 错误=0.0, 无代码=-1.0）

5.  运行 src/train_grpo_single.py --config configs/local_grpo_single.yaml
6.  监控 tensorboard → 确认 reward 上升趋势
7.  运行 src/evaluate.py --tag baseline → 记录原始模型分数
8.  运行 src/evaluate.py --tag grpo_single → 对比提升
9.  运行 src/train_grpo_multi.py --config configs/local_grpo_multi.yaml
10. 运行 src/evaluate.py --tag grpo_multi → 对比多轮提升
11. 本地验收：pipeline 全流程跑通不报错，reward 曲线上升
```

### 云端阶段（80GB GPU，出正式结果）

```
1.  租 AutoDL 80GB 实例 → 配置环境 → pip install veRL + 依赖
2.  ModelScope 内网下载 Qwen3-4B
3.  上传代码（git clone 或 scp）
4.  生成训练数据（MBPP train+val + APPS-easy-cleaned，--full）
5.  tmux 内运行：单轮 GRPO（200步, batch=128, seed=42）
6.  事后评估：对 4 个 checkpoint 逐一跑 HumanEval+ / MBPP+ → 选择最优
7.  tmux 内运行：多轮 AgentLoop GRPO（200步, batch=128, seed=42）
8.  事后评估：同上，选择最优多轮 checkpoint
9.  跑 3 组消融实验（奖励对比 / num_gen 对比 / 单轮 vs 多轮）
10. 打包最优 checkpoint → scp 传回本地
11. 本地 5070Ti 运行 LiveCodeBench 评估（结果好放报告，不好不放）
12. 收集所有结果 → 运行 analysis/ 脚本生成图表
13. 制作最终结果表格 + 截图
```

---

## 12. 成本预算

### 本地阶段
- 硬件：自有 5070Ti 12GB，**0 元**
- 电费：约 2-3 天训练，忽略不计

### 云端阶段

| 项目 | 规格 | 单价 | 用时 | 费用 |
|------|------|------|------|------|
| 单轮 GRPO 训练 | 80GB GPU | ~3-5 元/小时 | ~4-6 小时 | ~15-30 元 |
| 多轮 GRPO 训练 | 80GB GPU | ~3-5 元/小时 | ~8-12 小时 | ~30-60 元 |
| 消融 A（加法奖励） | 80GB GPU | ~3-5 元/小时 | ~4-6 小时 | ~15-30 元 |
| 消融 B（n=4/n=8） | 48GB GPU | ~2 元/小时 | ~8-12 小时 | ~16-24 元 |
| 评估（各 benchmark） | 48GB GPU | ~2 元/小时 | ~4-6 小时 | ~8-12 元 |
| APPS 数据清洗（DeepSeek API） | — | ~0.001 元/千token | ~3000条 | ~1 元 |
| 存储 | 50GB 数据盘 | ~0.5 元/天 | ~5 天 | ~2.5 元 |
| **总计** | | | | **~85-130 元** |

- 省钱建议：用 tmux 防断连；评估可以用低配实例；及时释放不用的高配实例

---

## 13. 常见问题排错指南

| 错误 | 原因 | 解决方案 |
|------|------|---------|
| veRL import 报错 | 版本不兼容或未安装 | `pip install verl>=0.4` 并对照官方文档确认 API |
| CUDA OOM | num_generations 或 max_completion_length 太大 | 减小 num_generations 到 4，max_completion_length 到 512 |
| reward 全是负数 | 模型代码能力差，全部执行失败 | 检查 system prompt 格式，确认 Qwen3 是否正确进入思考模式 |
| reward 不变化 | lr 太小或 num_generations 太少 | 增大 lr 到 1e-5，增大 num_generations |
| KL 散度爆炸 | 策略偏离参考模型太远 | 减小 lr，增大 KL penalty 系数，检查梯度裁剪 |
| AgentLoop 超时 | async 沙盒未正确 await | 确认 `execute_code` 是 `async def`，使用 `await` 调用 |
| vLLM 启动失败 | GPU 显存不足或版本不匹配 | 检查 vLLM 版本与 CUDA 版本兼容性；本地回退 HF generate |
| EvalPlus 报错 | 版本不兼容或 jsonl 格式错误 | `pip install --upgrade evalplus`，检查 completion 字段格式 |
| LiveCodeBench 加载失败 | 网络问题或 API 变更 | 检查网络，查看 LiveCodeBench GitHub 最新文档 |
| Docker GPU 直通失败 | NVIDIA Container Toolkit 未安装 | `sudo apt install nvidia-container-toolkit && sudo systemctl restart docker` |
| parquet 数据格式错误 | veRL 要求特定列名 | 确认 prompt 列为 chat messages JSON 字符串，extra_info 列含 test_cases |
| 模型不生成思考过程 | Qwen3 思考模式未激活 | 检查 tokenizer 的 chat_template 是否包含 `/think` 指令 |
| 云端模型下载慢 | 用了 HuggingFace 而非 ModelScope | 改用 `modelscope.snapshot_download()` |

---

## 14. 附录

### 14.1 面试关键知识点

**GRPO 算法**
- 与 PPO 的区别：不需要 value network，用组内相对优势代替 baseline
- 核心公式：advantage = (reward - group_mean) / group_std
- 优势：实现简单，不需要训练 critic，显存占用更低
- 面试要点：为什么 GRPO 适合代码任务（离散奖励、不需要稠密 value 估计）

**门控奖励 vs 加法奖励**
- 门控：代码提取失败 → 所有子奖励归零，防止 reward hacking
- 加法：各子奖励独立累加，可能出现"格式完美但代码全错"仍得正分
- 面试要点：门控设计避免了模型走捷径（只学格式不学正确性）

**多轮纠错**
- 核心创新：模型能根据执行错误反馈自我修正
- turn_penalty 设计：鼓励一次做对，但也奖励成功纠错
- 与简单数据增强的区别：训练时真正进行多轮交互，reward 信号更准确

**veRL 框架**
- 字节跳动开源，专为 post-training 设计
- 支持 GRPO/PPO/DPO 等多种算法
- AgentLoop：原生 async 多轮交互支持
- rollout 后端可切换：HF generate（省显存）/ vLLM（提速）

**LoRA 高效微调**
- 为什么用 LoRA 而不是全参数：省显存、训练快、不容易遗忘
- target_modules 选择：q/k/v/o_proj + gate/up/down_proj（注意力 + FFN 全覆盖）

**沙盒安全**
- subprocess 隔离 + timeout 防死循环 + RLIMIT_CPU 3s（内核级强制 CPU 时间限制）
- 面试讨论点：评估过 AST 静态扫描拦截 import os/subprocess 的方案，但 Python 的 exec/eval/`__import__`/ctypes 可绕过任何静态分析，且 sys 模块在编程题中是合法依赖会误杀。最终选择 Docker 隔离作为更可靠的防御层

### 14.2 与已有工作对比

| 工作 | 框架 | 模型规模 | 奖励设计 | 多轮 | 本项目差异 |
|------|------|---------|---------|------|-----------|
| DeepSeek-R1 | 自研 | 671B MoE | 规则奖励 | 否 | 规模不同；本项目聚焦代码+多轮 |
| OpenCoder-GRPO | TRL | 6.7B | 执行奖励 | 否 | 框架不同；本项目加入多轮纠错 |
| CodeRL | 自研 | 350M~16B | 单元测试奖励 | 否 | 规模/框架不同；本项目有 agentic 多轮 |
| RLHF Code (Microsoft) | 自研 | 多种 | 执行+格式 | 否 | 奖励设计不同；本项目用门控奖励 |
| **本项目** | veRL | 1.7B/4B | 门控奖励 | 是（AgentLoop） | 轻量、可复现、多轮纠错为核心创新 |

---

## 15. 补充保障措施

### 15.0 AgentLoop 早期验证提醒

**在阶段零完成后、阶段一开始前**，必须花半天时间测试 veRL AgentLoop 的最小示例：

```bash
# 1. 确认 AgentLoop 可以 import
python3 -c "from verl.workers.agent.agent_loop import AgentLoopBase; print('AgentLoop import OK')"

# 2. 测试最小 run 示例（参考 veRL examples/ 下的 agent 相关示例）
# 3. 如果成功 → 继续阶段一/二
# 4. 如果失败 → 查看错误信息，检查 veRL 版本和 API 变更
# 5. 如果查阅源码后仍无法解决 → 立即切换到 Plan B（Section 15.1）
# 6. 不在 AgentLoop 调试上花费超过 1 天！
```

**为什么这么早验证？** AgentLoop 是多轮训练的核心依赖，也是最大的不确定性。如果它在最后阶段才发现不工作，前期所有多轮相关的工作（数据准备、奖励函数、训练配置）都会白费。尽早验证可以将损失降到最低。

### 15.1 Plan B：手动多轮 Rollout（AgentLoop 不可用时的回退方案）

veRL 的 AgentLoop 处于 Alpha 状态。如果 AgentLoop 无法正常工作：

**为什么没有 AgentLoop 就只能单轮？** veRL 的标准 rollout 只支持"输入 prompt → 生成 completion → 结束"的单轮流程，没有内置的"生成→执行→反馈→再生成"循环机制。AgentLoop 正是为解决此问题设计的。没有它，无法在训练循环中实现真正的多轮 RL 交互。

1. **先完成单轮 GRPO 训练**（不依赖 AgentLoop）
2. **用训练好的单轮模型做 best-of-n + 手动多轮纠错**：
   - 对每道题生成 n 个 completion → 沙盒执行
   - 失败的：将错误反馈拼入 prompt → 重新调用 model.generate() → 再执行
   - 收集成功纠错的轨迹作为 SFT 数据
3. **用 SFT 数据做监督微调**（第二阶段）

> **已知局限**：Plan B 是单轮 RL + 多轮 SFT 的妥协方案，模型只学会"模仿纠错轨迹"而非"通过 RL 探索纠错策略"。面试时坦诚说明这一限制，展示对 RL vs SFT 区别的深入理解。
>
> 详见 agentic-rl-v2.md 第 6.8 节。

### 15.2 Checkpoint 事后评估 + 过拟合监控

不在训练循环中做自动早停（避免修改 veRL 框架），而是：

1. `save_freq=50` 保存 4 个 checkpoint（step 50/100/150/200）
2. 训练结束后，对每个 checkpoint 运行 HumanEval+ 和 MBPP+ 评估
3. 绘制 **step vs pass@1 曲线**，找到最优 checkpoint
4. 选择 pass@1 最高的 checkpoint 作为最终模型

过拟合信号：
- reward 上升但 eval pass@1 下降 → 过拟合
- KL 散度持续攀升 → 策略偏离过大
- 详见 agentic-rl-v2.md 第六-B 节。

### 15.3 LiveCodeBench 本地评估

云端训练完成后，将最优 checkpoint 下载到本地 5070Ti 12GB 进行 LiveCodeBench 评估：

- Qwen3-4B bf16 权重 ≈ 8GB，推理 KV cache ≈ 1-2GB，12GB 显卡可装载
- 结果好 → 放入技术报告，作为泛化性证据
- 结果不好 → 不放入报告，只展示 HumanEval+ 和 MBPP+ 结果
- 详见 agentic-rl-v2.md 第六-C 节。

### 15.4 可复现性保障

- 所有训练脚本统一 `seed=42`
- `requirements.txt` 锁定关键版本（torch==2.6.0, verl>=0.4, vllm>=0.8.0）
- 每次训练记录环境信息（git commit, Python/PyTorch/CUDA 版本）
- 详见 agentic-rl-v2.md 第六-D 节。
